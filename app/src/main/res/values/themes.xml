<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MasterSQL" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimaryDark">@android:color/holo_blue_dark</item>
    </style>

    <style name="Theme.MasterSQL" parent="Base.Theme.MasterSQL" />

    <style name="editTextAddCourse">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:background">@drawable/courses_buttons</item>
        <item name="android:backgroundTint">@color/gray</item>
    </style>

    <style name="editTextAddTextContent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:background">@drawable/courses_buttons</item>
        <item name="android:backgroundTint">@color/gray</item>
    </style>




</resources>